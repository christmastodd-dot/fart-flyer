{
  "name": "fart-flyer",
  "version": "1.0.0",
  "description": "Fart Flyer — pixel art farting flapper game",
  "scripts": {
    "sync":    "npx cap sync",
    "ios":     "npx cap open ios",
    "add-ios": "npx cap add ios && npx cap sync"
  },
  "dependencies": {
    "@capacitor/core":    "^6.1.0",
    "@capacitor/ios":     "^6.1.0",
    "@capacitor/haptics": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
